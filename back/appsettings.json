{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Settings": {
    "DbName": "${DB_NAME}",
    "DbUser": "${DB_USER}",
    "DbPassword": "${DB_PASSWORD}",
    "DbPort": "${DB_PORT}",
    "DbHost": "${DB_HOST}",
    "SecretKey": "${SECRET_KEY}",
    "CepAbertoToken": "${CEP_ABERTO_TOKEN}"
  },
  "AppDbContext": {
    
  }
}